name: debt_simplification
description: A Dart function for simplifying group debt settlements, similar to 'Quick Split'.
version: 1.0.0
homepage: https://github.com/i-aiymen/debt_simplification
repository: https://github.com/i-aiymen/debt_simplification

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
